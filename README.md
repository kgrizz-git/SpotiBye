# SpotifyPlaylistExporterV2

The goal is to split this into a server and a client. The server will handle the authentication and API calls, and the client will handle the UI.

## Build and Deploy

For desktop builds, signing guidance, GitHub workflows, and Cloudflare backend deployment, see `docs/build-and-deploy-guide.md`.
