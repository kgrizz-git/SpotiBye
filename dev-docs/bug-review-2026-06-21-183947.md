# SpotiBye Bug Assessment — 2026-06-21 18:39:47 UTC

> Full codebase audit of `src/backend/` (TypeScript, Cloudflare Worker) and `src/frontend/` (Python, Kivy/KivyMD).
> **Total: 44 issues** (22 backend, 22 frontend)

---

## Backend (TypeScript) — 22 Issues

### Critical

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| BE-SEC-1 | **Critical** | Security | `src/backend/services/jwt.ts:65` | JWT expiration bypass via falsy `exp` check. `if (decodedPayload.exp && ...)` skips expiration when `exp` is `0` (or any falsy value), accepting forged tokens. |
| BE-LOG-1 | **Critical** | Logic | `src/backend/services/spotify.ts:149` | Retry-After HTTP-date format causes `NaN` delay → busy spin. `parseInt(date_string)` returns `NaN`, resulting in 0ms retry during rate limiting. |

### High

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| BE-SEC-2 | High | Security | `src/backend/types/auth.ts:26` | `SessionData.spotify_data: any` stores raw PII (email, country) in KV without type safety. |
| BE-SEC-3 | High | Security | `src/backend/routes/auth.ts:14-16` | Unvalidated `redirect_uri` from client body — open redirect in OAuth flow. |
| BE-LOG-2 | High | Logic | `src/backend/services/spotify.ts:154-155` | Non-429 error responses discard body diagnostics. Only `statusText` is thrown; the Spotify JSON error message is lost. |
| BE-LOG-5 | High | Logic | `src/backend/index.ts:78-87` | `markFailed` not wrapped in try/catch inside queue consumer. If KV write fails, the message is never acked → infinite retries past `max_retries`. |
| BE-TYPE-1 | High | Type Safety | `src/backend/routes/spotify.ts:132` | `getPlaylistItemsHandler` typed as `c: any` — complete loss of type safety for shared handler. |
| BE-ERR-1 | High | Error Handling | `src/backend/routes/auth.ts:39-41,181-183,191-193` | Three empty catch blocks (OAuth init, logout, user info) swallow exceptions with no logging. |
| BE-ERR-4 | High | Error Handling | `src/backend/index.ts:80-83` | Same as BE-LOG-5 — `markFailed` uncaught in queue consumer. |

### Medium

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| BE-SEC-4 | Medium | Security | `src/backend/routes/auth.ts:143`, `services/spotify-auth.ts:102` | `(newTokens as any).refresh_token` bypasses type system; Spotify may return a new refresh token that gets silently dropped. |
| BE-SEC-5 | Medium | Security | `src/backend/services/spotify.ts:52` | `parseSpotifyResponse` validates top-level keys only; individual playlist items are not validated (`rawData.items as SpotifyPlaylist[]`). |
| BE-LOG-4 | Medium | Logic | `src/backend/services/analysis-job.ts:70-78` | Progress callback closes over stale KV snapshot; concurrent writes may lose fields. |
| BE-TYPE-2 | Medium | Type Safety | `src/backend/services/export.ts` (6+ loc) | Pervasive `any` params: `buildPlaylistMetadata(playlist: any)`, `buildExportTracks(allTracks: any[])`, etc. |
| BE-TYPE-3 | Medium | Type Safety | `src/backend/routes/export.ts` (8+ loc) | Pervasive `any` in route boundary functions: `resolveStepSize(body: any)`, etc. |
| BE-TYPE-5 | Medium | Type Safety | `src/backend/services/spotify.ts:109,116,135` | Double `as unknown as T` casts bypass TypeScript type checking. |
| BE-ERR-2 | Medium | Error Handling | `src/backend/routes/auth.ts:117-120` | OAuth callback returns generic `"Failed to complete OAuth flow"` regardless of actual error. |
| BE-ERR-3 | Medium | Error Handling | `src/backend/middleware/auth.ts:30` | `JSON.parse(sessionData)` without try/catch — corrupted KV data produces unlogged crash. |
| BE-ERR-5 | Medium | Error Handling | `src/backend/services/spotify-auth.ts:9-11` | Constructor silently accepts empty credentials (`clientId || ''`), causing confusing 401 later. |
| BE-RACE-1 | Medium | Race | `src/backend/middleware/auth.ts:33-54` | Concurrent token refresh during access token expiry — second refresh uses invalidated old refresh token. |
| BE-API-4 | Medium | Config | `tsconfig.json:11` + `.eslintrc.json:18` | `strict: true` undermined by `no-explicit-any: "warn"` (not error). |

### Low

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| BE-LOG-3 | Low | Logic | `src/backend/services/analysis.ts:110` | Pagination edge case with playlists containing only local tracks. |
| BE-LOG-6 | Low | Logic | `src/backend/services/export.ts:522-528` | `playlist` can be `undefined` in `buildPlaylistMetadata`; totals may mismatch. |
| BE-LOG-7 | Low | Logic | `src/backend/services/export.ts:1106` | Buffer detection divergence between Node and Workers `nodejs_compat`. |
| BE-TYPE-4 | Low | Type Safety | `src/backend/middleware/auth.ts:22` | Redundant `as JWTPayload` cast (method already returns correct type). |
| BE-RACE-2 | Low | Race | `src/backend/services/analysis-job.ts:38-44` | KV replication race window (15s wait); job can be prematurely sent to DLQ. |
| BE-DEAD-1 | Low | Dead Code | `src/backend/middleware/error.ts:26-42` | Unreachable error branches (`ValidationError`, `UnauthorizedError`, etc.) — never thrown. |
| BE-DEAD-2 | Low | Dead Code | `src/backend/services/analysis.ts:334-354` | `calculateAverageAudioFeatures` defined but never called. |
| BE-API-1 | Low | API Contract | `src/backend/index.ts:28-34` | Health check returns `{status, service, timestamp}` instead of `{data: ...}` wrapper. |
| BE-API-2 | Low | API Contract | `src/backend/routes/auth.ts:161-163` | Refresh endpoint returns duplicate `token` and `access_token` fields (same JWT value). |
| BE-API-3 | Low | Logic | `src/backend/services/export.ts:522-524` | Misleading conditional — playlist fetch guarded on ID mismatch that should never occur. |

---

## Frontend (Python) — 22 Issues

### Critical

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| FE-CRIT-1 | **Critical** | Stub in Prod | `src/frontend/services/reccobeats_backend.py:207-209` | `get_multiple_track_audio_features_safe` contains hardcoded test data for `"cached_track_1"` / `"cached_track_2"` and returns empty results for all real track IDs. Never queries a backend cache. |
| FE-CRIT-2 | **Critical** | Security | `src/frontend/config/backend_config.py:12-14` | Default `BACKEND_URL` hardcodes `spotibye-backend-development.kevin-grizzard.workers.dev` — exposes developer's Cloudflare Worker in production. |

### High

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| FE-HIGH-1 | High | Crash | `src/frontend/services/backend_client.py:253-256` | `get_playlist_tracks` calls `.get()` on response. If backend returns `{"data": [list]}` (standard wrapper), `_make_request` strips `data` → returns bare `list` → `AttributeError` on `.get()`. |
| FE-HIGH-2 | High | Logic | `src/frontend/screens/main_screen_filenames.py:52-61` | `increment_filename_suffix` uses naive `str.endswith("_2")` through `"_5"` — corrupts filenames ending in digits (e.g., `song_35.xlsx` → silent no-op; `song_14.xlsx` → `song_1_5.xlsx`). |
| FE-HIGH-3 | High | Drift Risk | `src/frontend/caching/backend_cache.py:258-273` | `clear_active_export_job` duplicates path-hashing logic instead of using shared helper — potential cache file drift. |
| FE-HIGH-4 | High | Logic | `src/frontend/caching/backend_cache.py:426-436` | `_get_basic_cache_stats` checks `isinstance(cached_data, list)` but cache files are always `{"data": [...], ...}` dicts — check always fails. Additionally, method is dead code (never called). |

### Medium

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| FE-MED-1 | Medium | API Contract | `src/frontend/services/backend_client.py:392` | `download_export` accepts `export_id` parameter but never uses it in the URL. |
| FE-MED-2 | Medium | Thread Safety | `src/frontend/ui/backend_playlist_card.py:557` | `_update_analysis_ui` modifies Kivy widgets without `@mainthread` decorator — crash risk if called from background thread. |
| FE-MED-3 | Medium | Resource Leak | `src/frontend/utils/platform_utils.py:119-129` | tkinter `Tk()` root window not destroyed on exception — `winfo_screenwidth()` / `winfo_screenheight()` can throw leaving zombie window on macOS. |
| FE-MED-4 | Medium | Error Handling | `src/frontend/services/backend_client.py:392-496` | Three download methods (`download_export`, `download_batch_export`, `download_export_job`) bypass `_make_request` — no retry/timeout/JSON error parsing. ~70 lines of duplicated error handling. |
| FE-MED-5 | Medium | Error Handling | `src/frontend/services/backend_client.py:500-505` | `health_check` catches only `BackendAPIError`; all other exceptions (`JSONDecodeError`, `TypeError`) propagate and crash callers. |
| FE-MED-6 | Medium | Logic | `src/frontend/screens/main_screen_logout.py:77-85` | `perform_logout` silently no-ops if running app lacks `logout` method — credentials remain in memory. |

### Low

| ID | Severity | Category | File:Line | Issue |
|----|----------|----------|-----------|-------|
| FE-LOW-1 | Low | Logic | `src/frontend/screens/backend_main_screen_adapter.py:1041-1048` | `clear_cache("playlists.json")` uses literal filename; cache files are prefix-hashed (`abc123_playlists.json`) — clears nothing. |
| FE-LOW-2 | Low | Validation | `src/frontend/config/backend_config.py:151-153` | `is_valid_backend_url` only checks `str.startswith(("http://", "https://"))` — accepts `"https://not a url!!!"`. |
| FE-LOW-3 | Low | Dead Code | `src/frontend/screens/backend_main_screen_adapter.py:86-106` | `_format_backend_api_error` has redundant `isinstance(details_payload, dict)` checks. |
| FE-LOW-4 | Low | Performance | `src/frontend/app/backend_app.py:206-234` | `_setup_fonts` re-imports `LabelBase` inside inner loop despite outer import failure — wasted work. |
| FE-LOW-5 | Low | Dead Code | `src/frontend/screens/main_screen_export.py:56-66` | `_build_backend_output_path` with `multiple=True` is an identity function (passthrough). |
| FE-LOW-6 | Low | Type | `src/frontend/screens/backend_main_screen_adapter.py:34-36` | `Optional[callable]` uses bare `callable` (builtin) instead of `Callable[..., Any]` (type). |
| FE-LOW-7 | Low | Shadowing | `src/frontend/utils/network_utils.py:29-32` | Custom `TimeoutError` shadows Python's builtin; `reccobeats_backend.py:125` raises the builtin, bypassing the custom network error handler. |
| FE-LOW-8 | Low | Error Handling | `src/frontend/ui/backend_cache_explorer.py:21-29` | Silently swallows `ImportError` — user sees "Backend: Not Available" with no diagnostics. |
| FE-LOW-9 | Low | Logic | `src/frontend/screens/main_screen_cache.py:66-93` | `clear_all_cache` shows "Cache Cleared" success popup but never actually clears the cache. |

---

## Summary by Category

| Category | Count | Critical | High | Medium | Low |
|----------|-------|----------|------|--------|-----|
| Security | 5 | 2 | 2 | 1 | — |
| Logic / Edge Cases | 10 | 1 | 3 | 2 | 4 |
| Type Safety | 5 | — | 1 | 3 | 1 |
| Error Handling | 10 | — | 2 | 4 | 4 |
| Race Conditions | 2 | — | — | 1 | 1 |
| API Contract / Stub | 5 | 1 | 1 | 2 | 1 |
| Dead Code / Drift | 5 | — | 1 | — | 4 |
| Thread Safety | 1 | — | — | 1 | — |
| Resource Leak | 1 | — | — | 1 | — |
| **Total** | **44** | **4** | **10** | **15** | **15** |

## Priority Fix Order

1. **BE-SEC-1** — JWT expiration bypass (security-critical)
2. **FE-CRIT-2** — Exposed dev URL in default config
3. **BE-LOG-1** — Rate-limit busy spin (DoS risk)
4. **FE-CRIT-1** — Hardcoded test data in ReccoBeats service
5. **BE-LOG-5 / BE-ERR-4** — Queue consumer infinite retry loop
6. **FE-HIGH-1** — Crash on playlist track fetch shape mismatch
7. **FE-HIGH-2** — Filename corruption for digit-ending names
8. **BE-ERR-1** — Silent empty catch blocks (3 instances)
9. **BE-SEC-3** — Unvalidated redirect_uri (open redirect)
10. **BE-RACE-1** — Concurrent token refresh race
