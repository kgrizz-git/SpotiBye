# Testing Skill

**Activate when:** User mentions "test", "pytest", "vitest", "coverage", or changes files in `src/backend/tests/` or `src/frontend/tests/`

**Available bundles:**
- `backend-testing.md`: Backend testing with vitest
- `frontend-testing.md`: Frontend testing with pytest

**Usage:** Load these bundles when writing or debugging tests.
