name: Deploy Backend

on:
  push:
    branches: [main]
    tags: ['v*']
    paths:
      - 'src/backend/**'
      - '.github/workflows/deploy-backend.yml'
      - '.github/workflows/security.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: read
  deployments: write
  security-events: read

jobs:
  # Only run tests if triggered manually. Push to main/tags skip this job
  # (ci.yml already validates PRs and pushes); deploy jobs accept skipped test.
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: github.event_name == 'workflow_dispatch'
    steps:
    - name: Checkout code
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

    - name: Setup Node.js
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
      with:
        node-version-file: '.nvmrc'

    - name: Install dependencies
      working-directory: src/backend
      run: npm ci

    - name: Run tests
      working-directory: src/backend
      run: npm run test:run
      env:
        ENVIRONMENT: test
        JWT_SECRET: test-jwt-secret-for-testing-only
        SPOTIFY_CLIENT_ID: test-client-id
        SPOTIFY_CLIENT_SECRET: test-client-secret

    - name: Run linting
      working-directory: src/backend
      run: echo "Linting skipped temporarily - ESLint configuration issues" # npm run lint

  deploy-dev:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: development
    # Deploy on main branch push or manual trigger, but allow skipped test job
    if: |
      (github.ref == 'refs/heads/main' && github.event_name == 'push') ||
      github.event_name == 'workflow_dispatch' &&
      (needs.test.result == 'success' || needs.test.result == 'skipped')

    steps:
    - name: Checkout code
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

    - name: Setup Node.js
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
      with:
        node-version-file: '.nvmrc'

    - name: Install dependencies
      working-directory: src/backend
      run: npm ci

    - name: Deploy to Cloudflare Workers (Development)
      working-directory: src/backend
      run: npm run deploy:dev
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

  deploy-prod:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    # Deploy on version tags, but allow skipped test job
    if: |
      startsWith(github.ref, 'refs/tags/v') &&
      github.event_name == 'push' &&
      (needs.test.result == 'success' || needs.test.result == 'skipped')

    steps:
    - name: Checkout code
      uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0

    - name: Setup Node.js
      uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
      with:
        node-version-file: '.nvmrc'

    - name: Install dependencies
      working-directory: src/backend
      run: npm ci

    - name: Deploy to Cloudflare Workers (Production)
      working-directory: src/backend
      run: npm run deploy:prod
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
